#!/usr/bin/env bash
# Change /usr/bin/bash to the more portable form: /usr/bin/env bash
# The next line creates a new file
# sed -e 's/\/bin\/bash/\/usr\/bin\/env bash/g' docksal.d7 >docksal.d7.new
# by using the -i option the edits are written to the file(s)

# use the find command and say xorgs to sed the contents of directories
