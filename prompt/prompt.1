#!/bin/bash
# change the command prompt 
# \w show working directory
# \u shows username
# \h shows hostname
# export will change the prompt for the current session
# the square brackets are odd, the @ can be something else
# and the $ sign too
export PS1='[\u@\h \w]$'
